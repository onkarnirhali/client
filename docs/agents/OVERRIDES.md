# Overrides (client)

- UI wording must stay aligned with freemium packaging and privacy disclosures.
- Source labels are user facing; confidence score is internal only.
- Keep responsive behavior for desktop and mobile web.
- Frontend changes must preserve consistency with shared theme and existing component language.
- Do not introduce a new UI framework/state library without explicit goal mapping and approval.
- Never expose secrets, tokens, or sensitive payload data in UI, logs, or client storage.
