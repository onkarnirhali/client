# Public env vars for the frontend (Vite only exposes VITE_* to the client)

# Base URL of the backend API. For local dev, point to your local API; for prod, use your Render URL.
VITE_API_BASE_URL=http://localhost:4000

