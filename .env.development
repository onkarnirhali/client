# Frontend needs only the public API base; keep AI/Gmail secrets on the server (.env does not use VITE_* for those).
# Use same-origin + Vite proxy when developing behind the tunnel.
VITE_API_BASE_URL=
